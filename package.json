{
  "name": "nic-milligan-creative",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/generate-content.js && npx serve src -p 3000",
    "build": "node scripts/build.js",
    "preview": "npx serve dist -p 3001"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
